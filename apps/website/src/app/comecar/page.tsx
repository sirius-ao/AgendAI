import { AccessPreview } from '@/components/common/AccessPreview';
export const metadata = { title: 'Começar grátis', robots: { index: false, follow: true } };
export default function Signup() {
  return <AccessPreview mode="comecar" />;
}
