# Docker Compose local. In Coolify, configure this as both a build and runtime variable.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Host port for local Docker Compose; the container always listens on 3000.
WEBSITE_PORT=3000
